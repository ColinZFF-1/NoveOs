--- a/book.yaml
+++ b/book.yaml
@@ -59,14 +59,14 @@ writing:
   tolerance: 300
   max_retries: 1
 
-# DeepSeek V4 Flash @ 硅基流动
+# DeepSeek-V3 @ 硅基流动
 llm:
-  model: kimi-k2.5
+  model: deepseek-ai/DeepSeek-V3
   api_key: "${OPENAI_API_KEY}"
-  api_base: https://api.moonshot.cn/v1
+  api_base: "${OPENAI_API_BASE}"
   temperature: 0.7
   max_tokens: 16000
   timeout: 300
 
-# Fallback：同样 V4 Flash @ 硅基流动
+# Fallback：同样 DeepSeek-V3 @ 硅基流动
 llm_fallback:
-  model: kimi-k2.5
+  model: deepseek-ai/DeepSeek-V3
   api_key: "${OPENAI_API_KEY_FALLBACK}"
-  api_base: https://api.moonshot.cn/v1
+  api_base: "${OPENAI_API_BASE}"
   temperature: 0.7
   max_tokens: 16000
   timeout: 300
